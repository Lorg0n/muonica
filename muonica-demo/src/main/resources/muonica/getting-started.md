# Getting started

Create an order to verify that your client can authenticate and send JSON requests.

::: endpoint POST /orders
:::

Use the interactive request panel to replace the generated values before sending the request.
