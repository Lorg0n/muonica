# List user activity

Returns recent events associated with a user account. Activity records are intentionally small and append-only: consumers can display them in a timeline without knowing the implementation details of the profile service.

The optional `limit` parameter controls how many events a client requests. Use a conservative value for mobile views and a larger value for support tooling.

:::slot parameters
:::

:::slot responses
:::
