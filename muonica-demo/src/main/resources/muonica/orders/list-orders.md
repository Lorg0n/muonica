# List orders

Returns a filterable collection of orders for the current account. Combine `status` and `customerId` for a focused view, then use `page` and `pageSize` for a table or operations dashboard.

The request id header is optional but recommended. It gives clients a way to correlate a slow list request with server logs and downstream calls.

:::slot parameters
:::

:::slot responses
:::
